#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main(){
    int num = 6;
    printf("%f", 8.9);
    printf("\n%f", 8.9 + 5.2); //operating with floating point numbers
    printf("\n%d", 8 + 5); //operating with integer, it's the same to operate with floating point
    printf("\n%d", num);
    printf("\n%f", pow(2,3)); //Function that calculate de pow of a number a in order b
    printf("\n%f", sqrt(2)); //Function that calculate de square of the number
    printf("\n%f", ceil(36.356)); // funciton that gives highest next number
    printf("\n%f", floor(5.55)); //function that gives the lowest next number


}